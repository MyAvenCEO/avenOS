{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "vault-webview",
	"description": "Isolated vault child webview embedded in main — Stronghold secrets + read-only self",
	"webviews": ["vault-embed"],
	"permissions": ["vault:default", "self:vault-webview-self", "core:event:default"]
}
