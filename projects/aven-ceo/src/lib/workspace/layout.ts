/** Single max width for workspace header, page columns, and floating composers. */
export const workspaceContentClass = 'mx-auto w-full max-w-6xl'

/** Wider shell for /me three-column orchestrator mock. */
export const workspaceOrchestratorClass = 'mx-auto w-full max-w-[min(100%,88rem)]'
