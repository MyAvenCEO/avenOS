/**
 * HITL UI is dispatched in {@link HitlTodoHost} by `HitlTodo['type']`.
 * Add a new variant: extend `HitlTodo` in types.ts, add a Svelte card under `hitl/`, branch in Host.
 */

export type { HitlTodo, HitlTodoType } from './types'
