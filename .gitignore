node_modules

# Local second brain vault + upstream mirrors (never commit)
/.data/
/projects/aven-ceo/.data/
/.repos/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/projects/*/.svelte-kit
/lib/*/.svelte-kit
/build
/projects/*/build
/lib/*/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Jaensen local SQLite (do not commit)
/.jaensen/state.db
/.jaensen/state.db-*

# Python
__pycache__/
*.py[cod]
*.pyo

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Vibe app sandbox + mini-app build outputs (rebuilt by `bun run build` per package).
/libs/vibe-app-sandbox/sandbox/dist/
/libs/vibe-apps/*/dist/

# Rust / Cargo (all crates in repo)
target/
**/target/

# Repo-local P2p signal relay seed (<repo>/.avenOS/dev/p2p-signal)
/.avenOS/
