---
description: Spec an aven-board card — uncover the real goal, make it measurable (discover column)
argument-hint: <item-ref — number, slug, or a fresh idea to spec>
---
Invoke the **discover** skill (.claude/skills/discover) on this aven-board item:
interview to uncover the real goal, make "done" provable from command output,
write the spec, and promote the card into `libs/aven-board/board/discover/`.

Item: $ARGUMENTS
