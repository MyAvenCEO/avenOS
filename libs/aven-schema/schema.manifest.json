{
	"tables": {
		"peers": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"nullable": true,
					"comment": "routing — caps-only sync. null = device-local trust-set row (not synced); admin-identity id = network roster row (syncs to admin-identity members)"
				},
				{
					"name": "signer_did",
					"type": "text",
					"plaintext": true,
					"comment": "did:key — peer/member identity"
				},
				{
					"name": "device_label",
					"type": "text",
					"nullable": true,
					"comment": "e.g. MacBook M1 — self-published device label (admin-identity roster) / local label (trust-set)"
				},
				{
					"name": "account_name",
					"type": "text",
					"nullable": true,
					"comment": "self-published member display name in the admin-identity roster (null for plain trust-set rows). Sealed-by-default so a blind replica can't read names — see board idea 0009."
				},
				{
					"name": "kind",
					"type": "text",
					"plaintext": true,
					"comment": "local | remote"
				},
				{
					"name": "added_at_ms",
					"type": "bigint",
					"plaintext": true,
					"comment": "local allowlist metadata"
				},
				{
					"name": "status",
					"type": "text",
					"plaintext": true,
					"comment": "pending | active | revoked"
				}
			]
		},
		"keyshares": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — keyshare lookup"
				},
				{
					"name": "dek_version",
					"type": "bigint",
					"plaintext": true,
					"comment": "routing — DEK version line"
				},
				{
					"name": "recipient_did",
					"type": "text",
					"plaintext": true,
					"comment": "routing — recipient DID"
				},
				{
					"name": "wrapper_did",
					"type": "text",
					"plaintext": true,
					"comment": "routing — granter DID for KEK derivation"
				},
				{
					"name": "wrapped_dek",
					"type": "text",
					"plaintext": true,
					"comment": "routing — wrapped DEK blob"
				}
			]
		},
		"safes": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — SAFE row identity (self-owned)"
				},
				{
					"name": "type",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "human | aven | spark — the kind of SAFE"
				},
				{
					"name": "safe_did",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "did:safe:<uuid> — stable public SAFE identifier"
				},
				{
					"name": "wrap_did",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "did:key of the SAFE wrap keypair — the public key granters wrap this SAFE's inbound DEK keyshares to (the did:safe: member primitive)"
				},
				{
					"name": "wrap_privkey_b64",
					"type": "text",
					"nullable": true,
					"comment": "sealed — ed25519 seed of the SAFE wrap keypair; only members (DEK holders) open it to unwrap SAFE-addressed keyshares"
				},
				{
					"name": "username_slug",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "human-identity profile handle (null for aven)"
				},
				{ "name": "name", "type": "text", "comment": "sealed at rest" },
				{
					"name": "issuer_pubkey_b64",
					"type": "text",
					"comment": "sealed — biscuit root Ed25519 public key (URL-safe base64)"
				},
				{ "name": "genesis_b64", "type": "text", "comment": "sealed — biscuit chain" },
				{
					"name": "current_dek_version",
					"type": "bigint",
					"plaintext": true,
					"comment": "routing — active DEK line for seal/hydrate"
				},
				{
					"name": "created_at_ms",
					"type": "bigint",
					"plaintext": true,
					"comment": "identity row metadata"
				}
			]
		},
		"safe_controllers": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — the controlled SAFE; rows sync to exactly its members"
				},
				{
					"name": "controller_did",
					"type": "text",
					"plaintext": true,
					"comment": "did:safe:<uuid> of the controller this row copies"
				},
				{
					"name": "role",
					"type": "text",
					"plaintext": true,
					"comment": "owner | delegate | executor | reader"
				},
				{
					"name": "genesis_b64",
					"type": "text",
					"comment": "sealed at rest — COPY of the controller's biscuit chain, readable by THIS SAFE's members so they can resolve the did:safe: path offline (N-hop authorize + verify-on-apply)"
				},
				{
					"name": "issuer_pubkey_b64",
					"type": "text",
					"comment": "sealed at rest — the controller chain's root Ed25519 public key"
				},
				{
					"name": "added_at_ms",
					"type": "bigint",
					"plaintext": true,
					"comment": "controller admission timestamp"
				}
			]
		},
		"todos": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — ACC + sync"
				},
				{ "name": "title", "type": "text" },
				{
					"name": "done",
					"type": "text",
					"exposeTs": "boolean"
				},
				{ "name": "description", "type": "text", "nullable": true }
			]
		},
		"messages": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — biscuit + P2P sync"
				},
				{
					"name": "created_at_ms",
					"type": "text",
					"exposeTs": "bigint",
					"comment": "sealed at rest"
				},
				{
					"name": "author_did",
					"type": "text",
					"comment": "sealed at rest"
				},
				{
					"name": "body",
					"type": "text",
					"comment": "sealed at rest"
				},
				{
					"name": "role",
					"type": "text",
					"nullable": true,
					"comment": "sealed at rest; author kind: \"user\" | \"agent\" (null/absent on legacy rows = user). Plain text, NOT enum: the add-only migration lens fills a generic Text default that an enum's variant gate rejects (`expected Enum, got Some(Text)`)."
				}
			]
		},
		"files": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — ACC + sync"
				},
				{ "name": "intent_id", "type": "text", "comment": "sealed at rest" },
				{ "name": "filename", "type": "text", "comment": "sealed at rest" },
				{
					"name": "mime_type",
					"type": "text",
					"comment": "sealed at rest"
				},
				{
					"name": "size_bytes",
					"type": "text",
					"exposeTs": "bigint",
					"comment": "sealed at rest"
				},
				{
					"name": "created_at_ms",
					"type": "text",
					"exposeTs": "bigint",
					"comment": "sealed at rest"
				},
				{
					"name": "content",
					"type": "bytea",
					"comment": "sealed at rest — IPC JSON uses base64"
				}
			]
		},
		"memories": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — ACC + sync (one brain per SAFE)"
				},
				{
					"name": "stream",
					"type": "text",
					"plaintext": true,
					"comment": "artifact column / filter — surface the memory came through (talk | …)"
				},
				{
					"name": "author_role",
					"type": "text",
					"plaintext": true,
					"comment": "artifact column / filter — user | agent | system. Plain text, NOT enum (lens-friendly, see messages.role)"
				},
				{
					"name": "source",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "artifact column — origin row id (message/file row); joins + citations"
				},
				{
					"name": "seq",
					"type": "integer",
					"plaintext": true,
					"nullable": true,
					"comment": "artifact column — chunk position within source (neighbor expansion)"
				},
				{
					"name": "line_start",
					"type": "integer",
					"plaintext": true,
					"nullable": true,
					"comment": "artifact column — surgical citation range"
				},
				{
					"name": "line_end",
					"type": "integer",
					"plaintext": true,
					"nullable": true,
					"comment": "artifact column — surgical citation range"
				},
				{
					"name": "content_hash",
					"type": "bytea",
					"plaintext": true,
					"nullable": true,
					"comment": "dedup key — sha256 of plaintext content (the engine row digest covers the whole sealed row and can't serve content dedup)"
				},
				{
					"name": "content",
					"type": "text",
					"comment": "sealed at rest — verbatim evidence"
				},
				{
					"name": "embedding",
					"type": "vector",
					"dim": 768,
					"nullable": true,
					"comment": "sealed at rest — EmbeddingGemma 768-d packed f32; search via the E1 unseal-on-scan seam"
				},
				{
					"name": "content_date",
					"type": "text",
					"exposeTs": "bigint",
					"nullable": true,
					"comment": "sealed — domain time (when the content happened, not when written; write time = engine RowProvenance)"
				},
				{
					"name": "source_version",
					"type": "bigint",
					"plaintext": true,
					"nullable": true,
					"comment": "ingest bookkeeping — source revision (incremental skip)"
				},
				{
					"name": "normalize_version",
					"type": "integer",
					"plaintext": true,
					"nullable": true,
					"comment": "ingest bookkeeping — pipeline version (silent rebuild)"
				},
				{
					"name": "veracity",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "trust class — stated | inferred | imported | tool | unknown; recall score multiplier"
				},
				{
					"name": "superseded_by",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "re-weighting, never deletion — replacement memory row id; hot path filters IS NULL"
				}
			]
		},
		"entities": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — ACC + sync"
				},
				{
					"name": "name",
					"type": "text",
					"comment": "sealed at rest — extracted name (pure interpretation: no backing artifact row; else links point at the row directly)"
				},
				{
					"name": "kind",
					"type": "text",
					"plaintext": true,
					"comment": "person | topic | project | thing (plain text, lens-friendly)"
				},
				{
					"name": "properties",
					"type": "json",
					"nullable": true,
					"comment": "sealed at rest"
				}
			]
		},
		"links": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — ACC + sync"
				},
				{
					"name": "from",
					"type": "text",
					"plaintext": true,
					"comment": "endpoint — any row id (memory / entity / artifact row); table resolves via object metadata"
				},
				{
					"name": "to",
					"type": "text",
					"plaintext": true,
					"comment": "endpoint — any row id (memory / entity / artifact row)"
				},
				{
					"name": "kind",
					"type": "text",
					"plaintext": true,
					"comment": "registered link kind — mentions | summarizes | refers_to | assoc | <claim predicates>"
				},
				{
					"name": "class",
					"type": "text",
					"plaintext": true,
					"comment": "note | claim | bond — merge semantics, enforced by the brain's kind registry"
				},
				{
					"name": "valid_from",
					"type": "text",
					"exposeTs": "bigint",
					"nullable": true,
					"comment": "sealed — claim validity opens"
				},
				{
					"name": "valid_to",
					"type": "text",
					"exposeTs": "bigint",
					"nullable": true,
					"comment": "sealed — claim validity closes (null = currently true)"
				},
				{
					"name": "confidence",
					"type": "double",
					"plaintext": true,
					"nullable": true,
					"comment": "claim — Bayesian: conf += (1−conf)·w_veracity·0.3"
				},
				{
					"name": "strength",
					"type": "double",
					"plaintext": true,
					"nullable": true,
					"comment": "bond — Hebbian potentiation (+0.05 on co-access, floor 0.05, max 5.0)"
				},
				{
					"name": "stability",
					"type": "double",
					"plaintext": true,
					"nullable": true,
					"comment": "bond — Ebbinghaus decay: strength·exp(−days/stability)"
				},
				{
					"name": "access_count",
					"type": "bigint",
					"plaintext": true,
					"nullable": true,
					"comment": "bond — co-access count (Counter-merge once the merge-strategy hook lands)"
				},
				{
					"name": "last_access",
					"type": "bigint",
					"plaintext": true,
					"nullable": true,
					"comment": "bond — spaced-potentiation gate (≥1h)"
				},
				{
					"name": "source_memory",
					"type": "text",
					"plaintext": true,
					"nullable": true,
					"comment": "evidence — the memory row a claim was extracted from"
				}
			]
		},
		"context_traces": {
			"columns": [
				{
					"name": "owner",
					"type": "uuid",
					"plaintext": true,
					"comment": "routing — ACC + sync"
				},
				{
					"name": "message_id",
					"type": "text",
					"comment": "sealed — the user message row this context served"
				},
				{
					"name": "reply_id",
					"type": "text",
					"nullable": true,
					"comment": "sealed — the agent reply row"
				},
				{
					"name": "trace",
					"type": "text",
					"comment": "sealed — ContextTrace JSON: decision log (ids/via/rank/score/budget) + assembly watermark; state-as-of replays from engine row histories"
				},
				{
					"name": "created_at_ms",
					"type": "text",
					"exposeTs": "bigint",
					"comment": "sealed at rest"
				}
			]
		}
	}
}
