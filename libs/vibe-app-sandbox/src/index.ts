/**
 * Public surface for `@avenos/vibe-app-sandbox`:
 * - host glue: `loadSandboxProxy`, `createAppBridge`, `runApp`, `log`, types
 * - default sandbox URL helper for hosts running the bundled `serve.ts`
 */
export * from './host'
