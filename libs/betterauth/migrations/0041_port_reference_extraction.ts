import { type Kysely, sql } from 'kysely'

// board 0098 — PORT the legacy OCR reference extraction into avenOS: high-fidelity, battle-tested
// system prompts + rich doctype tool schemas for BOTH the invoice (`capture`) and bank-statement
// (`capture-bank`) extract nodes (from /Users/samuelandert/Documents/Development/ocr/config). The unified
// invoice prompt (extracts parties + header + body) is used since avenOS's enrichAddressbook handles the
// parties from one extraction. Persistence stays the Lojban predication architecture. Flow config only
// (Layer A); forward-only. aven-db untouched.

const INVOICE_PROMPT = "You are a document understanding engine for **invoices**. You MUST call the **emit** tool exactly once.\n\nThe tool schema (JSON parameters) is the sole source of truth for field names. Map printed labels semantically onto those parameters.\n\nFill **every** required field (`vendor`, `buyer`, **`header`**, `statements`). Under **`header`** put **`document_kind`**, **`currency`**, **`invoice_number`**, **`order_number`**, **`customer_number`**, dates (**`issue_date`**, **`due_date`**, **`letter_date`**), **`referenced_invoice_numbers`**, and optional **`reference_entries`** (one `{kind,label,value}` row per labeled line such as *Abrechnungsnummer*, *Gl\u00e4ubiger-ID*, *Ihre Referenz*, *Einlieferungszeitraum*, *Genutztes Versandprodukt*, sheet/ICR ranges \u2014 use **`kind`** for the normalized category, **`label`** for the printed heading). On the **document root**, put **`totals`** ( **`subtotal`**, **`tax_breakdown`**, **`tax_total`**, **`invoice_total`** ), **`payments`** (all amounts paid \u2014 **do not** duplicate a separate scalar for \u201calready paid\u201d), **`total_outstanding`**, and anything else the schema requires. **`statements[]`** carries **only** line blocks (`line_items`, `line_groups`, optional section headings). **Vendor imprint** **Service periods** \u2014 For each **`statements[]`** block, fill **`service_period_normalized`** (**YYYY-MM-DD** **`start_date`**/**`end_date`**, or **calendar_month**/**calendar_year**) when **Leistungszeitraum**/**billing period** is readable; keep **`service_period`** verbatim. For **`line_groups`**, mirror with **`context_period_normalized**. \u2014 Populate **`vendor.banking_accounts`** when creditor / SEPA blocks appear (IBAN/BIC + **`creditor_identifier`**; Lastschrift: **`purpose`** `direct_debit_collection`, **`sepa_mandate_reference`**). Put the **debit** account (*von Ihrem Konto*) in **`buyer.banking_accounts`** (`purpose` **`sepa_debit_debtor`**). Mirror labeled *Gl\u00e4ubiger-ID* / *Mandat* in **`header.reference_entries`** when not only in prose. Use **`vendor.org_public_record`** for imprint / legal footer: **locations**, **identifiers**, **contact_channels**, **officer_groups**. **Line units** \u2014 Preserve **`quantity_unit`** as printed; add **`quantity_measure`** when the unit is clear.\n\n**VAT / MwSt** \u2014 Footer tables with columns like *Netto*, *MwSt 19%* + amount must populate **`totals.tax_breakdown`**: one object **per printed rate**, each with numeric **`tax_rate_percent`** **and** **`tax_amount`** (plus `net_subtotal` when shown). Set **`totals.tax_total`** when one summed tax figure is printed.\n\n**Position rows (`line_items` / group `rows`)** \u2014 Populate **`quantity`**, **`quantity_unit`**, **`unit_price`**, **`title`**, **`description`**, **`amount`**, **`position`**, **`article_number`** when columns exist. **DRY titles:** When a position has a main headline (e.g. **1.** *3000 Bipro \u2026 2/2025*) and indented sub-lines (**a.**, **b.**), assign the headline to **`title`** and **only** sub-lines to **`description`** \u2014 **never duplicate** **`title`** text at the start of **`description`**; concatenate multiline sub-lines inside **`description`** with newlines (**escapes**: backslash+n) or **; ** separators. If the row has no headline/sub-line split, set **`title`** to null and the full phrase in **`description`**."

const BANK_PROMPT = "You are a document understanding engine for **bank / card / account statements**. Call the **emit** tool exactly once.\n\nThe tool schema is authoritative. Extract **account_holder**, **institution**, **`account_overview`**, period dates, balances, and **`transactions`**.\n\n**Credit-card tables (distinct columns)** \u2014 Do **not** mix these up:\n- **`original_currency`** + **`original_amount`** = **W\u00e4hrung** + **Betrag** in foreign money (e.g. USD 96,79).\n- **`exchange_rate`** = printed **Kurs** / **Umrechnungskurs** (e.g. 1,1573) \u2014 **one number/ratio**, not an EUR amount.\n- **`amount`** = **total booked in account currency (EUR)**: main **Betrag in EUR** plus the *x% f\u00fcr W\u00e4hrungsumrechnung* line when merged into one charge (signed).\n- **`fx_surcharge_eur`** = the **second EUR line** (*1,75% f\u00fcr W\u00e4hrungsumrechnung* surcharge in EUR), signed \u2014 **not** the Kurs (**`exchange_rate`**), not the USD **Betrag**.\n- **`foreign_exchange_fee_percent`** = the printed **W\u00e4hrungsumrechnungs** rate as a number (**`1.75`** for **1,75%**).\n\n**Separate JSON keys** \u2014 **`booking_date`**, **`value_date`**, **`foreign_exchange_fee_percent`**, **`fx_surcharge_eur`**, **`exchange_rate`**, **`amount`**, **`description`**: each scalar in its own property; **never** append `,fx_surcharge_eur:\u2026` into date strings. **Datum Beleg** \u2192 **`value_date`**; **Datum Buchung** \u2192 **`booking_date`**.\n\n**General** \u2014 Preserve **Vorzeichen** on amounts. Set **`booking_date_as_printed`** for *dd.mm.yy* when useful. Product labels (e.g. **CC SSKM**) \u2192 **`account_overview.product_name`** or **`notes`**.\n\nUse **null** only when truly absent."

const INVOICE_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["vendor", "buyer", "header", "statements"], "properties": {"header": {"type": "object", "additionalProperties": false, "required": ["document_kind"], "description": "Document header row: invoice identifiers, billing dates, ISO currency, and kind. Keep these fields together; do not duplicate them under `statements`.", "properties": {"document_kind": {"type": ["string", "null"], "enum": ["invoice", "payment_reminder", "consolidated_reminder", "credit_note", "receipt", "other", null], "description": "Invoice sub-type. Required. Choose the single best fit."}, "letter_date": {"type": ["string", "null"], "description": "Date on the letter or cover. YYYY-MM-DD if unambiguous."}, "due_date": {"type": ["string", "null"], "description": "Payment due date if stated for the document."}, "referenced_invoice_numbers": {"type": "array", "items": {"type": "string"}, "description": "Invoice or statement identifiers listed in the header — one string per number, never comma-merged."}, "issue_date": {"type": ["string", "null"], "description": "**Invoice / Rechnungs** issue date when printed (Rechnungsdatum, Ausstellungsdatum). YYYY-MM-DD. Not the service/billing period alone."}, "invoice_number": {"type": ["string", "null"], "description": "**Primary** invoice or Rechnungsnummer at **document root** (not under `statements`)."}, "order_number": {"type": ["string", "null"], "description": "**Order / Auftrag / Bestell** reference when distinct from the invoice #. Root field."}, "customer_number": {"type": ["string", "null"], "description": "**Printed customer / debtor account** at the issuer (Kunden-Nr., Kundennummer). Use **`reference_entries`** for carrier-specific **`Abrechnungsnummer`** and other labeled IDs that are not your generic Kunden-Nr.; do not stuff composite billing IDs into **`customer_number`**."}, "reference_entries": {"type": "array", "description": "Structured **labeled header references** beyond one-line scalars (**`invoice_number`**, **`order_number`**, …). One row per printed label/value (DRY schema, same shape for shipment, creditor, billing, batch, period). Omit empty rows.", "items": {"type": "object", "additionalProperties": false, "required": ["kind", "value"], "properties": {"kind": {"type": "string", "description": "**Normalized category** for filtering and dashboards; use printed **`label`** for exact wording.", "enum": ["creditor_identifier", "billing_number", "customer_identifier", "order_reference", "your_reference", "vendor_internal_reference", "sheet_batch_or_icr_range", "carrier_product_service", "delivery_pickup_period", "party_name_and_address_note", "sepa_mandate_reference", "payment_scheme_identifier", "other"]}, "sub_kind": {"type": ["string", "null"], "description": "Optional finer discriminator (SEPA Creditor-ID vs IBAN creditor, tariff name, tariff code)."}, "label": {"type": ["string", "null"], "description": "**Verbatim** printed caption (e.g. *Abrechnungsnummer*, *Gläubiger-ID*, *Ihre Referenz*, *Einlieferungszeitraum*)."}, "value": {"type": "string", "description": "Primary identifier or free-text value as printed."}, "value_end": {"type": ["string", "null"], "description": "**Optional.** End of a range (e.g. second sheet **248**) or ISO **YYYY-MM-DD** end date when **`value`** is the start (`value` / **`value_end`** should not duplicate a single fused string)."}}}, "default": []}, "currency": {"type": ["string", "null"], "description": "ISO 4217 code (EUR, USD, …)."}}}, "vendor": {"type": "object", "additionalProperties": false, "required": ["name"], "properties": {"identity_id": {"type": ["string", "null"], "description": "Resolved identity UUID from the party pipeline (for UI); not printed on the source document."}, "name": {"type": ["string", "null"], "description": "Legal entity name only (the contracting party), not a combined string with a contact person on the same line — use **contact** fields for a named representative."}, "contact_name": {"type": ["string", "null"], "description": "Ansprechpartner / contact person (natural person) when the document lists them next to the company; keep **name** to the company only."}, "contact_identity_id": {"type": ["string", "null"], "description": "UUID of a **person** identity when the contact is stored separately (from party step `vendor_representative`); links in the Identities view."}, "street": {"type": ["string", "null"], "description": "Street line only."}, "postal_code": {"type": ["string", "null"]}, "city": {"type": ["string", "null"]}, "country": {"type": ["string", "null"], "description": "Full country name in English (e.g. Germany, United States) — not a two-letter code. If the document shows a code, expand to the name."}, "email": {"type": ["string", "null"]}, "phone": {"type": ["string", "null"]}, "tax_id": {"type": ["string", "null"]}, "bank": {"type": ["object", "null"], "additionalProperties": false, "description": "Single **creditor bank** block; prefer **`banking_accounts`** when multiple IBANs or SEPA blocks appear — you may mirror the primary row here.", "properties": {"bank_name": {"type": ["string", "null"], "description": "Bank or branch name as printed."}, "iban": {"type": ["string", "null"], "description": "International Bank Account Number."}, "bic": {"type": ["string", "null"], "description": "Bank Identifier Code (SWIFT/BIC)."}}}, "banking_accounts": {"type": "array", "description": "**All printed bank / SEPA payment blocks** for the issuer (e.g. *Kontoverbindung*, separate direct-debit account). Prefer over a lone **`bank`** when more than one IBAN or when **Gläubiger-ID** is tied to collections. Omit or `[]` if absent.", "items": {"type": "object", "additionalProperties": false, "properties": {"purpose": {"type": ["string", "null"], "description": "Why this block exists when distinguishable.", "enum": ["payment_reception", "direct_debit_collection", "salary_or_internal", "other", null]}, "label_printed": {"type": ["string", "null"], "description": "Section heading as printed (*Kontoverbindung*, *Bankverbindung*)."}, "bank_name": {"type": ["string", "null"]}, "iban": {"type": ["string", "null"]}, "bic": {"type": ["string", "null"]}, "creditor_identifier": {"type": ["string", "null"], "description": "**SEPA Creditor identifier** (e.g. *Gläubiger-ID*) printed with or next to this account / mandate block."}, "sepa_mandate_reference": {"type": ["string", "null"], "description": "**SEPA mandate reference** (*Mandatsreferenz*, e.g. *Mandat* BOE…) when shown with Lastschrift / Bankeinzug for this creditor block."}}}, "default": []}, "org_public_record": {"type": ["object", "null"], "description": "Generic **imprint / legal disclosure** for the **vendor** (register seats, HRB, VAT, extra addresses, board lists, extra web/email/phone lines) — not the primary letterhead address (that stays on root **street** / **city** fields).", "additionalProperties": false, "properties": {"locations": {"type": "array", "description": "Additional structured addresses (billing office, registered seat, branch, *Abrechnungsstelle*, *Hausadresse*).", "items": {"type": "object", "additionalProperties": false, "required": ["role"], "properties": {"role": {"type": "string", "enum": ["principal_trading", "billing_service", "registered_seat", "branch", "returns_or_correspondence", "other"], "description": "Abstract role of this address block."}, "heading_printed": {"type": ["string", "null"], "description": "Verbatim printed heading above the address."}, "organization_name": {"type": ["string", "null"], "description": "Legal name line if it differs from the main **vendor.name** (e.g. parent *Deutsche Post AG* vs *DHL Paket GmbH*)."}, "street": {"type": ["string", "null"]}, "postal_code": {"type": ["string", "null"]}, "city": {"type": ["string", "null"]}, "country": {"type": ["string", "null"], "description": "Full English country name when known."}}}}, "identifiers": {"type": "array", "description": "Registers, VAT, national tax numbers, chamber IDs — one object per printed line.", "items": {"type": "object", "additionalProperties": false, "required": ["value"], "properties": {"category": {"type": "string", "enum": ["commercial_register", "vat_id", "national_tax_number", "trade_chamber", "court_filing", "other"]}, "issuer": {"type": ["string", "null"], "description": "Register court, city, or authority (*Registergericht Bonn*)."}, "value": {"type": "string", "description": "The identifier string (HRB…, USt-IdNr., …)."}, "label_printed": {"type": ["string", "null"], "description": "Caption as printed (*USt-IdNr.*, *St.Nr.*, *HRB*)."}}}}, "contact_channels": {"type": "array", "description": "Extra contact lines beyond the main **email** / **phone** (e.g. imprint *www.*, billing fax).", "items": {"type": "object", "additionalProperties": false, "required": ["channel", "value"], "properties": {"channel": {"type": "string", "enum": ["website", "email", "phone", "fax", "other"]}, "value": {"type": "string"}}}}, "officer_groups": {"type": "array", "description": "Management / board / supervisory lists when printed as named groups.", "items": {"type": "object", "additionalProperties": false, "properties": {"group_kind": {"type": "string", "enum": ["executives", "board", "supervisory_board", "other"]}, "heading_printed": {"type": ["string", "null"], "description": "e.g. *Geschäftsführung*, *Vorstand*."}, "names": {"type": "array", "items": {"type": "string"}}}}}}}}}, "buyer": {"type": "object", "additionalProperties": false, "required": ["name"], "properties": {"identity_id": {"type": ["string", "null"], "description": "Resolved identity UUID from the party pipeline (for UI); not printed on the source document."}, "name": {"type": ["string", "null"], "description": "Customer / bill-to **company or legal name** only — not a merged \"Name, Company\" string; the named individual belongs in **contact** fields and may be a separate `buyer_representative` identity."}, "contact_name": {"type": ["string", "null"], "description": "Ansprechpartner / contact or representative (person) for this bill-to, when shown separately on the document."}, "contact_identity_id": {"type": ["string", "null"], "description": "UUID of a **person** identity for the contact (from party step `buyer_representative`)."}, "street": {"type": ["string", "null"]}, "postal_code": {"type": ["string", "null"]}, "city": {"type": ["string", "null"]}, "country": {"type": ["string", "null"], "description": "Full country name in English (e.g. Germany, United States) — not a two-letter code. If the document shows a code, expand to the name."}, "email": {"type": ["string", "null"]}, "phone": {"type": ["string", "null"]}, "tax_id": {"type": ["string", "null"]}, "banking_accounts": {"type": "array", "description": "**Printed debtor / buyer bank rows** especially for **SEPA direct debit** (*Lastschrift*, *von Ihrem Konto*): IBAN/BIC/**bank_name** of the account **to be debited** — not the creditor’s receipt account (that stays under **`vendor.banking_accounts`**). Omit `[]` if absent.", "items": {"type": "object", "additionalProperties": false, "properties": {"purpose": {"type": ["string", "null"], "description": "Why this block exists when distinguishable.", "enum": ["sepa_debit_debtor", "payment_credit", "other", null]}, "label_printed": {"type": ["string", "null"], "description": "Section heading as printed if any."}, "bank_name": {"type": ["string", "null"]}, "iban": {"type": ["string", "null"]}, "bic": {"type": ["string", "null"]}}}, "default": []}}}, "payment_instructions": {"type": ["string", "null"], "description": "Verbatim **payment / bank prose** when useful; **also** fill **`vendor.banking_accounts`**, **`buyer.banking_accounts`**, and **`header.reference_entries`** for *Gläubiger-ID* / *Mandat* so data is not only buried here."}, "totals": {"type": ["object", "null"], "description": "Consolidated document-level money roll-ups: net subtotal, VAT by rate, tax sum, and gross total.", "additionalProperties": false, "properties": {"subtotal": {"type": ["number", "null"], "description": "Net subtotal (Zwischensumme / net before tax) for the **whole document** when one line is printed."}, "tax_breakdown": {"type": "array", "description": "When the document prints a **tax summary by rate** (e.g. *MwSt Gruppe* *A 19%* / *B 7%*, or columns *USt. %* + net + tax + gross), one object per **distinct** rate. Omit the array only if there is no per-rate table. Each row must give the **numeric** rate in `tax_rate_percent` (not only a letter).", "items": {"type": "object", "additionalProperties": false, "required": ["tax_rate_percent", "tax_amount"], "properties": {"tax_rate_percent": {"type": "number", "description": "**Required.** VAT / MwSt rate as a number (0–100) exactly as in the tax summary (e.g. 19, 7, 0).", "minimum": 0, "maximum": 100}, "tax_group_letter": {"type": ["string", "null"], "description": "Optional letter in *MwSt Gruppe* (e.g. *A* on the same line as *19%*).", "maxLength": 2}, "tax_rate_text": {"type": ["string", "null"], "description": "Optional verbatim label; `tax_rate_percent` remains the source of truth."}, "net_subtotal": {"type": ["number", "null"], "description": "Net / taxable base for this rate in document currency (e.g. Zwischensumme ohne USt.)."}, "tax_amount": {"type": "number", "description": "**Required** when the table shows a MwSt / VAT amount for this rate."}, "gross_subtotal": {"type": ["number", "null"], "description": "Gross for this rate if printed (optional; often derivable as net + tax)."}}}}, "tax_total": {"type": ["number", "null"], "description": "Total VAT / MwSt summed when a single roll-up is printed. **Whenever** the footer shows tax **by rate**, fill **`totals.tax_breakdown`** with one object per rate: **`tax_rate_percent`** (number 0–100) **and** **`tax_amount`** — never only a lump sum without per-rate rows when the table lists *MwSt 19%* + amount."}, "invoice_total": {"type": ["number", "null"], "description": "**Gross** document total (Brutto / Gesamtwert) before paid/outstanding split."}}}, "payments": {"type": "array", "description": "**All payments** toward this invoice — card/EC lines, transfers, or a **single roll-up row** when only a total paid is printed (same shape: `amount`, optional `date` / `method` / `reference`). Do **not** emit a separate scalar for amount paid. **No payments:** `[]`.", "items": {"type": "object", "additionalProperties": false, "required": ["amount"], "properties": {"date": {"type": ["string", "null"], "description": "Payment date if printed."}, "amount": {"type": ["number", "null"]}, "method": {"type": ["string", "null"], "description": "e.g. 'Überweisung', 'Lastschrift', 'Bar'."}, "reference": {"type": ["string", "null"], "description": "Reference / Verwendungszweck if given."}}}}, "total_outstanding": {"type": ["number", "null"], "description": "Document-level **amount still due** after payments. **Unpaid / open:** printed balance due, or **`totals.invoice_total`** if nothing paid yet. **Paid in full:** `0`. **Partial:** remaining balance vs **`totals.invoice_total`** minus the **sum of `payments[].amount`** when those figures appear."}, "statements": {"type": "array", "minItems": 1, "description": "Each separately headed or separately totaled billing block. Assign line items only to the block they appear under in the markdown.", "items": {"type": "object", "additionalProperties": false, "required": ["line_items"], "properties": {"section_title": {"type": ["string", "null"], "description": "Section heading as printed above this block."}, "service_period": {"type": ["string", "null"], "description": "Verbatim service / billing period line as printed (any language/format). Prefer also filling **service_period_normalized** with ISO dates when inferable."}, "line_items": {"type": "array", "minItems": 0, "description": "Flat list of itemized rows (use for simple tables). If you use `line_groups` for tiered/SaaS usage, set to `[]` to avoid duplicating the same lines. **`title`** and **`description`** must not duplicate the same headline — see field descriptions.", "items": {"type": "object", "additionalProperties": false, "required": ["description", "amount"], "properties": {"title": {"type": ["string", "null"], "description": "Main position headline when the source visually splits it from **indented sub-lines** (e.g. **a.**/**b.**, bullets under **1.**). Put **only** that headline here (product + period if it sits on the same main line). Use `null` if the row is one unbroken string — then put everything in **`description`**. **Must not** be repeated verbatim inside **`description`**."}, "description": {"type": ["string", "null"], "description": "Detail text **only**: indented sub-lines, fine print, refs — concatenate **multiline** blocks with **`\\n`** (or **`; `** if the PDF wraps one conceptual block). When **`title`** is set, **do not** echo **`title`** or repeat the headline here; include sub-lines (**a.** *Abrufe …*, **b.** *Weiterleitung …*) entirely. When **`title`** is null, carry the **full** line text here."}, "line_kind": {"type": ["string", "null"], "enum": ["detail", "discount", "subtotal", "header_amount", "note", "other", null], "description": "Row role when the source distinguishes it (e.g. discount lines). Omitted for normal lines."}, "quantity": {"type": ["number", "null"], "description": "**Anzahl / Menge** numeric quantity only (no unit string)."}, "quantity_unit": {"type": ["string", "null"], "description": "**Einheit verbatim** from the document (ST, Std., kg, Palette, …) — preserve odd spellings."}, "quantity_measure": {"type": ["string", "null"], "enum": [null, "count_each_piece", "count_pair", "count_dozen", "count_gross", "count_set", "pack_inner_pack", "pack_carton_case_box", "pack_crate_export", "pack_pallet", "pack_skid_platform", "pack_bundle_lot_batch", "pack_roll_coil_reel", "pack_sheet_flat", "pack_drum_barrel_keg", "pack_container_load", "mass_microgram", "mass_milligram", "mass_gram", "mass_kilogram", "mass_metric_tonne", "mass_pound", "mass_ounce", "length_millimeter", "length_centimeter", "length_meter", "length_kilometer", "length_inch", "length_foot", "length_yard", "length_mile", "area_square_millimeter", "area_square_centimeter", "area_square_meter", "area_square_kilometer", "area_square_inch", "area_square_foot", "area_square_yard", "area_hectare", "area_acre", "volume_cubic_millimeter", "volume_cubic_centimeter", "volume_cubic_meter", "volume_microliter", "volume_milliliter", "volume_liter", "volume_hectoliter", "volume_us_liquid_gallon", "volume_us_fluid_quart", "volume_us_fluid_ounce", "volume_imperial_gallon", "volume_imperial_fluid_ounce", "time_second", "time_minute", "time_hour", "time_day_calendar", "time_week_calendar", "time_month_calendar", "time_year_calendar", "energy_joule", "energy_kilojoule", "energy_megajoule", "energy_watt_hour", "energy_kilowatt_hour", "energy_megawatt_hour", "power_kilowatt", "digital_data_byte", "digital_data_kilobyte", "digital_data_megabyte", "digital_data_gigabyte", "digital_data_terabyte", "telecom_voice_minute", "telecom_sms_each", "telecom_data_megabyte", "freight_shipment_each", "freight_delivery_stop_each", "freight_loading_meter", "service_license_seat_each", "service_user_account_each", "service_subscription_period", "service_flat_charge_once", "labor_staff_hour", "labor_machine_hour", "unknown_or_ambiguous_unit", "other_invoice_line_unit"], "description": "**English-standard normalized unit family** (SI / IEC where applicable; US customary/imperial gallon where Anglophone invoicing prints them — still store **quantity_unit** verbatim). Map from the ME column when clear; otherwise null or **`other_invoice_line_unit`**."}, "unit_price": {"type": ["number", "null"], "description": "**Einzelpreis** / unit price before line VAT; column *Einzelpreis* / unit when present; paired with **`amount`** (Zeilen-Gesamt / line total)."}, "tax_rate_percent": {"type": ["number", "null"], "description": "VAT / MwSt rate as a **number** (e.g. 19, 7, 0) from the line or from the MwSt summary row that matches a letter on the line. Required whenever a rate is identifiable; null only if the line has no tax indication.", "minimum": 0, "maximum": 100}, "tax_group_letter": {"type": ["string", "null"], "description": "Single-letter **POS / MwSt group** (e.g. *A*, *B*) when printed on the line; always pair with `tax_rate_percent` from the receipt’s tax summary table (e.g. *A* → 19, *B* → 7 in DE).", "maxLength": 2}, "tax_rate_text": {"type": ["string", "null"], "description": "Verbatim tax fragment when useful (*MwSt 19%*, *reduced*). **Does not** replace `tax_rate_percent`."}, "amount": {"type": ["number", "null"], "description": "Line amount as printed."}, "position": {"type": ["string", "number", "null"], "description": "**Pos.** column / position label when printed (e.g. 10, 20, 40 rub-64b)."}, "article_number": {"type": ["string", "null"], "description": "**Artikel-Nr.** / supplier article code (Artikelnummer) when in its own column."}}}}, "line_groups": {"type": "array", "minItems": 0, "description": "Grouped line items by **position / category** (SaaS usage, usage tables). Each group is one printed block: **title** and optional **context** (period) are separate; **rows** are the detail lines only.", "items": {"type": "object", "additionalProperties": false, "required": ["rows"], "properties": {"title": {"type": ["string", "null"], "description": "Category / position name only (e.g. Compute Hours, Egress GB). Do not put the date range here."}, "context": {"type": ["string", "null"], "description": "Verbatim subline (period / caption) under the **title**. Prefer also **context_period_normalized** with ISO dates when inferable (same shape as **service_period_normalized**)."}, "rows": {"type": "array", "minItems": 0, "description": "Detail lines in this block (resources, discounts, subtotals as printed).", "items": {"type": "object", "additionalProperties": false, "required": ["description", "amount"], "properties": {"title": {"type": ["string", "null"], "description": "Primary cell label when the table splits headline vs detail (tier, SKU line). **`description`** must not repeat this text. `null` if one string only — put all of it in **`description`**."}, "description": {"type": ["string", "null"], "description": "Remainder only when **`title`** is set; **multiline** sub-rows allowed (**`\\n`**). Never duplicate **`title`**; if **`title`** is null, full row text."}, "line_kind": {"type": ["string", "null"], "enum": ["detail", "discount", "subtotal", "header_amount", "note", "other", null], "description": "discount / subtotal / detail as in the source table."}, "quantity": {"type": ["number", "null"], "description": "**Anzahl / Menge** numeric quantity only (no unit string)."}, "quantity_unit": {"type": ["string", "null"], "description": "**Einheit verbatim** from the document (ST, Std., kg, Palette, …)."}, "quantity_measure": {"type": ["string", "null"], "enum": [null, "count_each_piece", "count_pair", "count_dozen", "count_gross", "count_set", "pack_inner_pack", "pack_carton_case_box", "pack_crate_export", "pack_pallet", "pack_skid_platform", "pack_bundle_lot_batch", "pack_roll_coil_reel", "pack_sheet_flat", "pack_drum_barrel_keg", "pack_container_load", "mass_microgram", "mass_milligram", "mass_gram", "mass_kilogram", "mass_metric_tonne", "mass_pound", "mass_ounce", "length_millimeter", "length_centimeter", "length_meter", "length_kilometer", "length_inch", "length_foot", "length_yard", "length_mile", "area_square_millimeter", "area_square_centimeter", "area_square_meter", "area_square_kilometer", "area_square_inch", "area_square_foot", "area_square_yard", "area_hectare", "area_acre", "volume_cubic_millimeter", "volume_cubic_centimeter", "volume_cubic_meter", "volume_microliter", "volume_milliliter", "volume_liter", "volume_hectoliter", "volume_us_liquid_gallon", "volume_us_fluid_quart", "volume_us_fluid_ounce", "volume_imperial_gallon", "volume_imperial_fluid_ounce", "time_second", "time_minute", "time_hour", "time_day_calendar", "time_week_calendar", "time_month_calendar", "time_year_calendar", "energy_joule", "energy_kilojoule", "energy_megajoule", "energy_watt_hour", "energy_kilowatt_hour", "energy_megawatt_hour", "power_kilowatt", "digital_data_byte", "digital_data_kilobyte", "digital_data_megabyte", "digital_data_gigabyte", "digital_data_terabyte", "telecom_voice_minute", "telecom_sms_each", "telecom_data_megabyte", "freight_shipment_each", "freight_delivery_stop_each", "freight_loading_meter", "service_license_seat_each", "service_user_account_each", "service_subscription_period", "service_flat_charge_once", "labor_staff_hour", "labor_machine_hour", "unknown_or_ambiguous_unit", "other_invoice_line_unit"], "description": "**English-standard normalized unit family** (SI / IEC where applicable; US customary/imperial gallon where Anglophone invoicing prints them — still store **quantity_unit** verbatim). Map from the ME column when clear; otherwise null or **`other_invoice_line_unit`**."}, "unit_price": {"type": ["number", "null"], "description": "**Einzelpreis** / unit price before line VAT; column *Einzelpreis* / unit when present; paired with **`amount`** (Zeilen-Gesamt / line total)."}, "tax_rate_percent": {"type": ["number", "null"], "description": "Per-row MwSt / VAT as a **number**; if only a group letter is printed, take the % from the receipt’s MwSt summary. minimum 0, maximum 100.", "minimum": 0, "maximum": 100}, "tax_group_letter": {"type": ["string", "null"], "description": "Single letter on the line (*A*/*B*) with `tax_rate_percent` from the matching *MwSt Gruppe* row.", "maxLength": 2}, "tax_rate_text": {"type": ["string", "null"], "description": "Verbatim label if needed; does not replace `tax_rate_percent`."}, "amount": {"type": ["number", "null"], "description": "Line amount as printed."}, "position": {"type": ["string", "number", "null"], "description": "**Pos.** column / position label when printed (e.g. 10, 20, 40 rub-64b)."}, "article_number": {"type": ["string", "null"], "description": "**Artikel-Nr.** / supplier article code (Artikelnummer) when in its own column."}}}}, "context_period_normalized": {"type": ["object", "null"], "additionalProperties": false, "description": "Normalized **billing / service period** for analytics and sorting. Use **ISO 8601** **start_date** / **end_date** (**YYYY-MM-DD**, inclusive) when the source gives explicit dates. For **month-only** captions (**10.2025**, **Oct 2025**, **MM/YYYY**), set **calendar_year** and **calendar_month** and, when unambiguous, **start_date**/**end_date** as the **first** and **last** calendar day of that month unless the invoice states narrower limits. For **quarterly** captions (**Q3 2025**), use **calendar_year**, **calendar_quarter**, and derived **start_date**/**end_date** when you can infer standard Gregorian quarters. Omit or null when the document only gives non-parseable prose — rely on the verbatim **context** / **service_period** strings then.", "properties": {"start_date": {"type": ["string", "null"], "description": "Inclusive interval start (**YYYY-MM-DD**).", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"}, "end_date": {"type": ["string", "null"], "description": "Inclusive interval end (**YYYY-MM-DD**).", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"}, "calendar_year": {"type": ["integer", "null"], "minimum": 1900, "maximum": 2200, "description": "Gregorian **year** when the period is year- or quarter- or month-bounded."}, "calendar_month": {"type": ["integer", "null"], "minimum": 1, "maximum": 12, "description": "**1–12** month when caption is month precision without explicit day span."}, "calendar_quarter": {"type": ["integer", "null"], "minimum": 1, "maximum": 4, "description": "**1–4** quarter when caption is quarterly."}}}}}}, "service_period_normalized": {"type": ["object", "null"], "additionalProperties": false, "description": "Normalized **billing / service period** for analytics and sorting. Use **ISO 8601** **start_date** / **end_date** (**YYYY-MM-DD**, inclusive) when the source gives explicit dates. For **month-only** captions (**10.2025**, **Oct 2025**, **MM/YYYY**), set **calendar_year** and **calendar_month** and, when unambiguous, **start_date**/**end_date** as the **first** and **last** calendar day of that month unless the invoice states narrower limits. For **quarterly** captions (**Q3 2025**), use **calendar_year**, **calendar_quarter**, and derived **start_date**/**end_date** when you can infer standard Gregorian quarters. Omit or null when the document only gives non-parseable prose — rely on the verbatim **service_period**/**context** strings then.", "properties": {"start_date": {"type": ["string", "null"], "description": "Inclusive interval start (**YYYY-MM-DD**).", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"}, "end_date": {"type": ["string", "null"], "description": "Inclusive interval end (**YYYY-MM-DD**).", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"}, "calendar_year": {"type": ["integer", "null"], "minimum": 1900, "maximum": 2200, "description": "Gregorian **year** when the period is year- or quarter- or month-bounded."}, "calendar_month": {"type": ["integer", "null"], "minimum": 1, "maximum": 12, "description": "**1–12** month when caption is month precision without explicit day span."}, "calendar_quarter": {"type": ["integer", "null"], "minimum": 1, "maximum": 4, "description": "**1–4** quarter when caption is quarterly."}}}}}}}} as unknown as Record<string, unknown>

const BANK_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["statement_kind", "currency", "account_holder", "institution", "account_overview", "period_start", "period_end", "transactions"], "properties": {"statement_kind": {"type": ["string", "null"], "enum": ["periodic_account_statement", "fee_or_service_statement", "credit_card_statement", "savings_account_statement", "other", null], "description": "Category of statement. Required. Pick the single best fit."}, "statement_id": {"type": ["string", "null"], "description": "Statement or period identifier as printed (e.g. reference number, period code)."}, "statement_issue_date": {"type": ["string", "null"], "description": "Document issue or print date when shown separately from the transaction period. YYYY-MM-DD if unambiguous."}, "currency": {"type": ["string", "null"], "description": "ISO 4217 account currency."}, "period_start": {"type": ["string", "null"], "description": "First day of the statement period. YYYY-MM-DD if unambiguous."}, "period_end": {"type": ["string", "null"], "description": "Last day of the statement period. YYYY-MM-DD if unambiguous."}, "payment_due_date": {"type": ["string", "null"], "description": "Payment due or pay-by date, mainly on credit or charge card statements. YYYY-MM-DD if unambiguous."}, "opening_balance": {"type": ["number", "null"], "description": "Opening balance for the period (signed)."}, "closing_balance": {"type": ["number", "null"], "description": "Closing balance after all posted lines (signed)."}, "account_holder": {"type": "object", "additionalProperties": false, "description": "Account holder: same party shape as invoice `buyer` / `vendor` (structured address, not one blob). Filled from extraction and merged with the identity pipeline (`account_holder` role).", "required": ["name"], "properties": {"identity_id": {"type": ["string", "null"], "description": "Resolved identity UUID from the party pipeline (for UI)."}, "name": {"type": ["string", "null"], "description": "Legal name of the account holder (person or company) as printed."}, "contact_name": {"type": ["string", "null"], "description": "Contact or representative (natural person) when shown next to the holder company."}, "contact_identity_id": {"type": ["string", "null"], "description": "Person identity when stored separately from the holder row."}, "street": {"type": ["string", "null"], "description": "Street line only (building number, street name)."}, "postal_code": {"type": ["string", "null"]}, "city": {"type": ["string", "null"]}, "country": {"type": ["string", "null"], "description": "Full country name in English, or as printed; expand 2-letter codes to names when unambiguous."}, "email": {"type": ["string", "null"]}, "phone": {"type": ["string", "null"]}, "tax_id": {"type": ["string", "null"]}}}, "institution": {"type": "object", "additionalProperties": false, "description": "Bank or payment institution (letterhead / account servicer): same party field model as `account_holder` and invoice parties.", "required": ["name"], "properties": {"identity_id": {"type": ["string", "null"], "description": "Resolved identity UUID (for UI)."}, "name": {"type": ["string", "null"], "description": "Institution legal or trading name as printed (e.g. bank name)."}, "contact_name": {"type": ["string", "null"]}, "contact_identity_id": {"type": ["string", "null"]}, "street": {"type": ["string", "null"], "description": "Institution or branch address — street line."}, "postal_code": {"type": ["string", "null"]}, "city": {"type": ["string", "null"]}, "country": {"type": ["string", "null"], "description": "Full country name when shown."}, "email": {"type": ["string", "null"]}, "phone": {"type": ["string", "null"]}, "tax_id": {"type": ["string", "null"]}}}, "account_overview": {"type": "object", "additionalProperties": false, "description": "Account and product metadata only (IBAN, BIC, product, card). Party names and addresses go in `account_holder` and `institution`, not here.", "properties": {"branch_name": {"type": ["string", "null"], "description": "Branch, office, or service centre if printed (distinct from the legal institution name)."}, "iban": {"type": ["string", "null"], "description": "IBAN if shown for this account."}, "bic": {"type": ["string", "null"], "description": "BIC or SWIFT code (often next to IBAN)."}, "account_number": {"type": ["string", "null"], "description": "Domestic account number if shown separately from IBAN."}, "domestic_bank_code": {"type": ["string", "null"], "description": "Domestic bank routing (e.g. German BLZ, UK sort code) if printed and not part of a single IBAN line."}, "product_name": {"type": ["string", "null"], "description": "Product or account type label (e.g. Giro, Business, Tagesgeld)."}, "card_last_four": {"type": ["string", "null"], "description": "Last four digits of the card when a masked number is shown (e.g. …4242)."}}}, "transactions": {"type": "array", "description": "Posted **logical** rows in table order. **Credit/charge cards:** one object per card charge even when the issuer prints a **second EUR line** (*x% für Währungsumrechnung*) — merge into the same object: **`exchange_rate`** = **Kurs** (Umrechnungskurs), **`foreign_exchange_fee_percent`** = *x* from **x%**, **`fx_surcharge_eur`** = that surcharge in EUR, **`amount`** = total EUR.", "items": {"type": "object", "additionalProperties": false, "required": ["description", "amount"], "properties": {"booking_date": {"type": ["string", "null"], "description": "Booking / **posting date** column (**Datum Buchung** when two date columns exist). **ONLY** **YYYY-MM-DD** or **null** — never fuse **`fx_surcharge_eur`**, **`description`**, or other keys into this string."}, "booking_date_as_printed": {"type": ["string", "null"], "description": "Same booking/posting row as **`booking_date`** but **verbatim print style** (e.g. *dd.mm.yy*, *05.03.26*) when useful for localization or PHP/export pipelines — complements ISO **`booking_date`**."}, "value_date": {"type": ["string", "null"], "description": "**Value / receipt date** column (**Datum Beleg**, *Wertstellung* / Belegdatum) when printed **before or separate from** **`booking_date`**. **ONLY** **YYYY-MM-DD** or **null** — never concatenate fee text or other JSON keys (use **`fx_surcharge_eur`** separately)."}, "description": {"type": ["string", "null"], "description": "Verwendungszweck / merchant text only. Real newline between lines if needed (**`\\n`** in JSON). Do **not** append pseudo-JSON like `,fx_surcharge_eur:` here."}, "counterparty_name": {"type": ["string", "null"], "description": "Primary counterparty label when split from full description."}, "transaction_id": {"type": ["string", "null"], "description": "Foreign key to `.data/transactions/<id>.json`."}, "amount": {"type": ["number", "null"], "description": "Signed **total booked in account currency** (root **`currency`**, usually EUR): **Betrag in EUR** main cell **plus** **`fx_surcharge_eur`** when the statement posts them as one logical charge. **Preserve sign** (debits negative). Not the **Kurs** — use **`exchange_rate`**; not the foreign **Betrag** — use **`original_amount`** + **`original_currency`**."}, "fx_surcharge_eur": {"type": ["number", "null"], "description": "Second **Betrag in EUR** line for *x% für Währungsumrechnung* (Zuschlag/Gebühr on the conversion **— not** the **Umrechnungskurs**; that is **`exchange_rate`**). Signed (usually negative). **null** if only one EUR amount is printed."}, "foreign_exchange_fee_percent": {"type": ["number", "null"], "description": "**Währungsumrechnungs**-percentage as a **numeric rate** (*1,75%* → **`1.75`**, independent of **`exchange_rate`** / **Kurs**). Omit if the surcharge line does not show a percentage or wording is unreadable."}, "original_amount": {"type": ["number", "null"], "description": "**Betrag** in **foreign transaction currency** (**Währung** column, e.g. 96.79). Signed if the table uses signs. Pair with **`original_currency`**."}, "original_currency": {"type": ["string", "null"], "description": "**Währung** column as ISO 4217 (e.g. **USD**). Root **`currency`** is the **statement account** currency (e.g. **EUR**)."}, "exchange_rate": {"type": ["string", "null"], "description": "**Kurs** / **Umrechnungskurs** as printed (e.g. 1,1573) — rate from foreign currency to EUR. **Not** the *x%* EUR surcharge (**`fx_surcharge_eur`**) nor the USD **Betrag** (**`original_amount`**)."}, "balance_after": {"type": ["number", "null"], "description": "Running balance after this line if shown."}, "title": {"type": ["string", "null"], "description": "Short merchant / payee headline when the PDF uses two lines (**title** + **Verwendungszweck** detail). Do not repeat it in **`description`**."}}}}, "notes": {"type": ["string", "null"], "description": "Footnotes, interest summary, or other narrative not mapped to a transaction line."}}} as unknown as Record<string, unknown>

function asJson(v: unknown): unknown {
	return typeof v === 'string' ? JSON.parse(v) : v
}

async function setExtract(db: Kysely<unknown>, flowId: string, prompt: string, schema: Record<string, unknown>): Promise<void> {
	const res = await sql<{ nodes: unknown }>`SELECT nodes FROM flow WHERE id = ${flowId}`.execute(db)
	const row = res.rows[0]
	if (!row) return
	const nodes = asJson(row.nodes) as Array<Record<string, unknown>>
	for (const n of nodes) {
		if (n.actor !== 'extract_document') continue
		n.system_prompt = prompt
		n.schema = schema
	}
	await sql`UPDATE flow SET nodes = ${JSON.stringify(nodes)}::jsonb WHERE id = ${flowId}`.execute(db)
}

export async function up(db: Kysely<unknown>): Promise<void> {
	await setExtract(db, 'capture', INVOICE_PROMPT, INVOICE_SCHEMA)
	await setExtract(db, 'capture-bank', BANK_PROMPT, BANK_SCHEMA)
}

export async function down(_db: Kysely<unknown>): Promise<void> {
	// forward-only (prompt + schema config).
}
