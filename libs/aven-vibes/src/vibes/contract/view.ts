// The contract viewer reuses the generic structured-document view; shape comes from `mapper.ts`.
// Re-exported so each doctype has a `view.ts`. board 0064.
export { docView as contractView } from '../_doc/view.js'
