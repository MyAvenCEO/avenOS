{
	"name": "@avenos/aven-ontology",
	"private": true,
	"version": "26.7.18-next.2",
	"type": "module",
	"description": "Pure, declarative CRUD/query engine over x1–x5 predications — register a type (bundle spec) → generic mutate + Datalog-style projection. No domain code per type. board 0088.",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "bun test ./test",
		"check": "tsc -p tsconfig.json --noEmit"
	}
}
