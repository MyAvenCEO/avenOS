// @avenos/skills — the AvenOS code-skills package. Each sub-skill is its own subpath export
// (import from '@avenos/skills/composer'); this barrel namespaces them. board 0056.
export * as composer from './composer/index'
