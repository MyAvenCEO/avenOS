{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "desktop-hyperswarm-peer",
	"description": "Hyperswarm / peer invite commands (macOS + Linux only; iOS v1 uses stub plugin + no IPC scope).",
	"windows": ["main"],
	"platforms": ["macOS", "linux"],
	"permissions": ["peer:default"]
}
