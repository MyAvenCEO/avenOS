# schema

Implementation package for the tree-explorer schema backend area.

## Contents

- schema domain helpers
- schema validation service
- schema registry/actor implementation
- bundled default extraction schema bootstrap helpers

## Boundary

This package provides schema implementation logic. Public schema-facing contracts
remain in `schema-contracts`.
