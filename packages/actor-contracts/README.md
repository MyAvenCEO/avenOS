# actor-contracts

Shared actor-facing contract vocabulary for Aven.

This package holds small, runtime-agnostic public protocol types that are reused across subsystems, including actor addresses, reply addresses, correlation metadata, base message shapes, shared error descriptors, and naming helpers.
